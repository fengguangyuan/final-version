varying vec2 UV;
varying vec3 Position_worldspace;
varying vec3 Normal_worldspace;

uniform sampler2D TexSampler;

void main()
{
	gl_FragData[0]		= vec4(texture2D(TexSampler,UV).rgb, 0);
	gl_FragData[1]		= vec4(Position_worldspace,0);
	gl_FragData[2]		= vec4(Normal_worldspace,0);
}